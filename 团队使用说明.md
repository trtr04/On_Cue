# On Cue 团队源码包

这是当前已经整合到统一 UI 的可运行源码，供队员分别修改模块。不要直接互相覆盖整个压缩包；每个人只修改自己负责的文件，改完后把修改后的文件和变更说明交给负责人合并。

## 启动方式

电脑需要 Node.js 20 或更高版本。在本目录打开终端后运行：

```bash
npm install
npm run dev
```

浏览器打开终端显示的本地地址（通常是 `http://localhost:3000`）。

提交修改前运行：

```bash
npm run test:oncue
npm run build
```

## 模块分工

### 1. 录音分析 / 知识库模块

主要文件：

- `knowledge-analysis.js`
- `public/knowledge/`
- `classic-training/zenmeban-dialogue-advisor/`
- `classic-training/backend/`

### 2. 经典训练 / 对话训练模块

主要文件：

- `training-game.js`
- `classic-training/frontend/`
- `classic-training/scenarios/`
- `classic-training/data/`

### 3. 情绪暴击 / 小老鼠游戏模块

主要文件：

- `public/mouse-game.html`

该页面来自原游戏资源。修改游戏玩法时优先在这个独立页面内完成，不要重画或替换原有小老鼠 IP。

### 4. 主界面 / 手机适配 / 模块接入

主要文件：

- `index.html`
- `styles.css`
- `app.js`
- `responsive.js`
- `app/`

这些是多人都会用到的公共文件，指定一名整合负责人修改，其他队员不要同时改，以免互相覆盖。

## 交付规则

每位队员交回修改时请附上：

1. 修改了哪些文件。
2. 实现了什么功能。
3. 如何测试。
4. 是否改动 `index.html`、`styles.css` 或 `app.js`。

不要把 `node_modules`、`.next`、`dist` 或个人 `.env` 文件发回；这些文件已经从团队包中排除。
